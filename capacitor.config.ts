import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bracezin.kiosk',
  appName: 'Bracezin Kiosk',
  webDir: 'dist/kiosk/browser',
};

export default config;
